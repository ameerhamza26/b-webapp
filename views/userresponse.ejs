<% include partials/head %>
<body>
    <% include partials/header %>
    
    <div>
        <div class="banner">
                <div class="container">
                    <a href="/home" class="float-left banner-link">
                        <img src="../../svg/back.svg"> BACK TO CAUSES</a>
                    <a href="/causes/edit/<%=causes[0].ID%>" class="float-right banner-link">EDIT CAUSE</a>
                    <div class="py-5 text-center">
                        <h2 class="lead"><%=causes[0].Title%></h2>
                    </div>
                </div>
            </div>
            <div class="container full-page">
            <% var selectedCauseId= causes[0].ID; %>
        <% include partials/sidemenu %>

        <div class="col-md-8 order-md-2">
            <h4 class="mb-3">User Response</h4>
           
            <div class="list-group">
                <label for="title" class="col-md-3 control-label">Select survey</label>
                <div class="col-md-9">
                    <select id="survey-response-select" class="form-control" name="countryId" id="drpdwn">
                        <option value="">Select survey</option>
                        <%for(var i=0;i< data.length;i++){ %>
                            <option value='<%= data[i].ID %>' />
                            <%=data[i].Title%>
                                </option>
                                <% }%>
                    </select>
                </div>
            </div>
            <div  id="user-response-div">

            </div>
            
        </div>

    </div>

    <% include partials/footer %>

</body>


