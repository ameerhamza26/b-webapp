<% include partials/head %>
<body>
    <% include partials/header %>
    
    <div class="container full-page">
        <div class="py-5 text-center">
            <h2 class="lead mt-5">Welcome to Burma cause management</h2>
        </div>

        <% include partials/sidemenu %>

        <div class="col-md-8 order-md-2">
            <h4 class="mb-3">Edit Event</h4>
            <form class="form-horizontal" role="form" method="post" action="/events/edit/<%=data[0].ID%>">
                <% if (message.length > 0) { %>
                    <div class="alert alert-success col-sm-12">
                        <%= message %>
                    </div>
                    <% } %>
                        <div id="signupalert" style="display:none" class="alert alert-danger">
                            <p>Error:</p>
                            <span></span>
                        </div>
                        <div class="form-group">
                            <label for="title" class="col-md-3 control-label">Title</label>
                            <div class="col-md-9">
                                <input type="text" class="form-control" name="title" placeholder="Title" value="<%=data[0].Title%>">
                            </div>
                        </div>
                        <div class="form-group">
                            <label for="title" class="col-md-3 control-label">Select cause</label>
                            <div class="col-md-9">
                                <select class="form-control" name="cause" id="drpdwn" value="<%= data[0].CauseId %>">
                                    <option value="">Select Cause</option>
                                    <%for(var i=0;i< causes.length;i++){ %>
                                        <%if (causes[i].ID == data[0].CauseId) { %>
                                            <option value='<%= causes[i].ID %>' selected />
                                            <% } else {  %>
                                                
                                                 <option value='<%= causes[i].ID %>' />
                                            <% } %>
                                        <%=causes[i].Title%>
                                            </option>
                                            <% }%>

                                            

                                </select>
                            </div>
                        </div>
                        <div class="form-group">
                            <label for="location" class="col-md-3 control-label">Location</label>
                            <div class="col-md-9">
                                <input type="text" class="form-control" name="location" placeholder="Location" value="<%=data[0].Location%>">
                            </div>
                        </div>
                        <div class="form-group">
                            <label for="start_date" class="col-md-3 control-label">Start Date</label>
                            <div class="col-md-9">
                                <input type="date" class="form-control" name="start_date" placeholder="Start date" value="<%= moment(data[0].StartDate).format( 'YYYY-MM-DD') %>">
                            </div>
                        </div>
                        <div class="form-group">
                            <label for="end_date" class="col-md-3 control-label">End Date</label>
                            <div class="col-md-9">
                                <input type="date" class="form-control" name="end_date" placeholder="End date" value="<%= moment(data[0].EndDate).format( 'YYYY-MM-DD') %>">
                            </div>
                        </div>
                        <div class="form-group">
                            <label for="notes" class="col-md-3 control-label">Notes</label>
                            <div class="col-md-9">
                                <textarea class="form-control" name="notes" placeholder="Notes"><%=data[0].Notes%></textarea>
                            </div>
                        </div>

                        <div class="form-group">
                            <!-- Button -->
                            <div class="col-md-offset-3 col-md-9">
                                <button id="btn-signup" type="submit" class="btn btn-primary">
                                    <i class="icon-hand-right"></i> &nbsp Submit</button>
                            </div>
                        </div>
            </form>
        </div>
    </div>

    <% include partials/footer %>
</body>



